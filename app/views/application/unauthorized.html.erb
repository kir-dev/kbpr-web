<div class="flex flex-col w-full gap-10 mb-10 items-center max-w-xl mx-auto">
  <h1 class="text-2xl font-medium">Ehhez az oldalhoz nincs jogousultságod!</h1>
  <%= image_tag 'warning.svg', class: 'w-full ' %>
  <% if current_user.blank? %>
    <p>Próbáld meg újra bejelentkezés után:</p>
    <%= button_to "Belépés", user_authsch_omniauth_authorize_path, data: { turbo: false }, class: 'btn w-full text-center', form_class: 'w-full' %>
    <p>Vagy</p>
  <% end %>
  <p>Jelezd egy körtagnak a problémát!</p>
  <%= authorized_link_to 'Vissza a főoldalra', root_path, class: 'btn w-full text-center' %>
</div>
