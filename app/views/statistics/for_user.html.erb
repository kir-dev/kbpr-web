<div class="mb-8">
  <div class="flex justify-between">
    <h1 class="text-4xl font-bold"><%= @user.name %> rendelései</h1>
    <%= link_to 'Vissza', statistics_users_path, class: 'btn-outline' %>
  </div>

    <div class="mt-6 bg-secondary rounded-md m-auto p-4">
        <div class="">
            <% @stat.each do |item, number| %>
                <p class="oneline">
                <span><%= item.name %></span>
                <strong>
                    <%= number %>
                </strong>
                </p>
            <% end %>
        </div>
    </div>
</div>
